/* @ Author: unknowntpo at 2020.2.24 (Mon)
 * @ Title: Structure tag;
 * @ Short description:
 *      Try with structure tag and see whether it will 
 *      allocate memory when the structure tag is defined.
 */
#include <stdio.h>

struct stdtype {
    char name[26];
    int id_number;    
};



int main() {
    struct stdtype x, y, z;   
}
